/**
 * Re-export shim: the wordmark data URIs moved to @validity.ai/verify-report (the
 * shared design system) so every surface — report, trends, compare,
 * dashboard — brands from one place. Import from @validity.ai/verify-report directly
 * in new code.
 */
export { WORDMARK_DARK_DATA_URI, WORDMARK_LIGHT_DATA_URI } from '@validity.ai/verify-report';
